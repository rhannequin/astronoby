# frozen_string_literal: true

source "https://rubygems.org"

gemspec

# Temporary: track horologium main until 0.1.0 is published. Drop this, and
# set `horologium` to `~> 0.1` in the gemspec, before merging.
gem "horologium", github: "rhannequin/horologium"
